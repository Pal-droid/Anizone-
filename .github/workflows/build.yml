name: Build Anizone Android Binaries

on:
  workflow_dispatch:
    inputs:
      architecture:
        description: 'Architecture to build'
        required: true
        default: 'all'
        type: choice
        options:
          - all
          - arm64
          - armv7
          - x64

jobs:
  build:
    name: Build for ${{ matrix.arch }}
    runs-on: ubuntu-latest
    if: github.event.inputs.architecture == 'all' || github.event.inputs.architecture == matrix.arch
    strategy:
      matrix:
        arch: [arm64, armv7, x64]
        include:
          - arch: arm64
            target: linux-arm64-20.11.0
            output: anizone-android-arm64
          - arch: armv7
            target: linux-armv7-20.11.0
            output: anizone-android-armv7
          - arch: x64
            target: linux-x64-20.11.0
            output: anizone-android-x64
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install pnpm
        uses: pnpm/action-setup@v3
        with:
          version: 8

      - name: Install dependencies
        run: pnpm install

      - name: Build Next.js app
        run: |
          echo 'module.exports = { output: "standalone" }' > next.config.js
          NEXT_TURBOPACK=0 pnpm build

      - name: Prepare standalone
        run: |
          cd .next/standalone
          pnpm install --prod

      - name: Install nexe
        run: npm install -g nexe

      - name: Build binary
        run: nexe .next/standalone/server.js --target ${{ matrix.target }} --output ${{ matrix.output }}

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.output }}
          path: ${{ matrix.output }}
